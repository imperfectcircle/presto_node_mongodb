class User {
    constructor(email, password, fullname, street, cap, city) {
        this.email = email;
        this.password = password;
        this.name = fullname;
        this.address = {
            street,
            cap,
            city,
        };

        signup()
    }
}
