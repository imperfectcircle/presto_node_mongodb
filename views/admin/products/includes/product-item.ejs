<article class="product-item">
    <img src="<%= product.imageUrl %>" alt="<%= product.title %>">
    <h2><%= product.title %></h2>
    <div>
        <a class="btn btn-outline" href="/admin/products/<%= product.id %> ">View & Edit</a>
        <button class="btn btn-outline">Delete</button>
    </div>
</article>